# SPDX-FileCopyrightText: 2026 2026 Oliver Lorenz
#
# SPDX-License-Identifier: AGPL-3.0-or-later

default:
    @{{ just_executable() }} --list --justfile {{ justfile() }}

upstream_url := "git@github.com:mother-of-all-self-hosting/mash-playbook.git"
upstream_remote_name := "upstream"

_ensure_upstream_remote:
    #!/usr/bin/env sh
    ORIGIN_URL=$(git remote get-url origin 2>/dev/null)
    if [ "$ORIGIN_URL" = "{{ upstream_url }}" ]; then
        echo "'origin' and 'upstream' are the same url."
        exit 1
    fi

    # Check, whether the remote 'upstream' already exists
    if git remote | grep -q "^{{ upstream_remote_name }}$"; then
        echo -n ""
    else
        echo "Add remote '{{ upstream_remote_name }}'"
        git remote add "{{ upstream_remote_name }}" "{{ upstream_url }}"

        if [ $? -eq 0 ]; then
            echo "Successfully added: {{ upstream_url }} as {{ upstream_remote_name }}"
        else
            echo "Error while adding {{ upstream_remote_name }} remote."
            exit 1
        fi
    fi

_merge_from_upstream:
    #!/usr/bin/env sh

    CURRENT_BRANCH=$(git rev-parse --abbrev-ref HEAD)

    if [ "$CURRENT_BRANCH" != "main" ]; then
        echo "merge from {{ upstream_remote_name }}/main can only run if you are on the local main branch"
        exit 1
    fi

    # if ! git diff-index --quiet HEAD --; then
    #     echo "Error: You have local not saved changes. Please stash or commit."
    #     exit 1
    # fi

    # Remembers how you resolved past conflicts and re-applies the same
    # resolution automatically the next time the identical conflict shows up
    # (e.g. in role code that both upstream and we touch).
    git config rerere.enabled true

    echo "Start sync: {{ upstream_remote_name}}/main to local main branch"
    git fetch upstream

    echo "Merge from {{ upstream_remote_name}}/main"
    if ! git merge upstream/main --no-edit; then
        REMAINING=$(git diff --name-only --diff-filter=U)
        if [ -n "$REMAINING" ]; then
            echo ""
            echo "Merge stopped with unresolved conflicts (not auto-resolvable via merge=union/rerere):"
            echo "$REMAINING" | sed 's/^/  - /'
            echo ""
            echo "Resolve them, 'git add' the files, then 'git commit' to finish the merge."
            exit 1
        fi
    fi

    # merge=union (see .gitattributes) resolves nearby insertions in
    # templates/setup.yml and templates/group_vars_mash_servers automatically,
    # but if both sides touched the exact same block, it keeps both instead
    # of conflicting. Surface that so it can be cleaned up manually.
    DUPES=$(cat templates/setup.yml templates/group_vars_mash_servers 2>/dev/null \
        | grep -o '# role-specific:[A-Za-z0-9_-]*' | sort | uniq -d)
    if [ -n "$DUPES" ]; then
        echo ""
        echo "Warning: duplicate role-specific blocks after union-merge, please de-duplicate manually:"
        echo "$DUPES" | sed 's/^/  - /'
    fi

    echo "merge done. Changes are local. You have to push it to origin/main by yourself"

update: _ensure_upstream_remote _merge_from_upstream
    @echo "Update from upstream done."
