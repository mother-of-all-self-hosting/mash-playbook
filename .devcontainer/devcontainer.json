{
	"name": "mash-playbook",
	"build": {
		"context": ".",
		"dockerfile": "Dockerfile"
	},
	"mounts": [
		"type=bind,source=/home/${localEnv:USER}/.ssh,target=/home/vscode/.ssh,readonly"
	],
	"customizations": {
		"vscode": {
			"extensions": ["redhat.ansible", "nefrob.vscode-just-syntax"]
		}
	},
	"postCreateCommand": "pre-commit install && just roles"
}
